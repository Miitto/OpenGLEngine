#pragma once

#include <glm/glm.hpp>

namespace engine {
  /// <summary>
  /// Engine forward vector.
  /// </summary>
  const glm::vec3 FORWARD(0, 0, -1);
  /// <summary>
  /// Engine up vector.
  /// </summary>
  const glm::vec3 UP(0, 1, 0);
  /// <summary>
  /// Engine right vector.
  /// </summary>
  const glm::vec3 RIGHT(1, 0, 0);
} // namespace engine